// i want a functio nthat displays sdoem message that returns a html tag and thells you wahat is wrong

function displayError(message = "some error happend") {
    return `<div class="error">${message}</div>`;

}